import RestaurantList from '../../components/RestaurantList'
import { Styled } from './RestaurantContainer.styled'

const RestaurantContainer = () => {
  return (
    <Styled>
      <RestaurantList />
    </Styled>
  )
}

export default RestaurantContainer
